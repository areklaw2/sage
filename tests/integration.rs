// End-to-end integration tests live here once M5 (agent core) is implemented.
// M2 infrastructure tests (insert/exists/dedup) are in src/db.rs since they
// need direct access to the binary crate's modules.
